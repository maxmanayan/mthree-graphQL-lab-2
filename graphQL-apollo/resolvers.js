const Author = require("../models/Author");
const Book = require("../models/Book");

const resolvers = {
  // Queries ----------------------------------
  // Mutations ----------------------------------
};

module.exports = { resolvers };
